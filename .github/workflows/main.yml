on: [push]

jobs:
  weblog_import:
    runs-on: ubuntu-latest
    name: weblog.lol importer
    steps:
      - uses: actions/checkout@v3
      - id: weblog_import
        uses: newbold/gh_actions_test@v9
        env:
          WEBLOG_API_KEY: ${{ secrets.WEBLOG_API_KEY }}
