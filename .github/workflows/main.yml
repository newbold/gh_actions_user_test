on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: weblog.lol importer
    steps:
      - uses: actions/checkout@v3
      - id: foo
        uses: newbold/gh_actions_test@v7
#        with:
#          who-to-greet: 'Mona the Octocat'
#      - run: echo random-number ${{ steps.foo.outputs.random-number }}
#        shell: bash
